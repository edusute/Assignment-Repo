#include <stdio.h>

int main() {
    char name[] = "Alex";
    int age = 17;
    float gpa = 3.5;

    printf("Name: %s\n", name);
    printf("Age: %d\n", age);
    printf("GPA: %.1f\n", gpa);

    return 0;
}
